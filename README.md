#### Newspaper API ETL
This project implements a simple ETL process to extract data from the Newspaper.org API (https://newsapi.org/), transform the data by dropping some columns and loading it into a PostgreSQL database.

